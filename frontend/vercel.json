{
  "buildCommand": "yarn install && yarn build",
  "outputDirectory": "build",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "RENDER_BACKEND_URL/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
